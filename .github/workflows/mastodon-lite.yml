name: mastodon-lite-post
on: [push]
jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: rzr/mastodon-lite/extra/github@master
        with:
          message: "https://github.com/rzr/mastodon-lite# has been updated, feedback welcome"
          access-token: ${{ secrets.MASTODON_ACCESS_TOKEN }}
